<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res/auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#000000"
    tools:context=".MainActivity">

    <!-- Header Section -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="#1a1a1a"
        android:padding="16dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Camera Stream App"
            android:textColor="#ffffff"
            android:textSize="24sp"
            android:textStyle="bold"
            android:gravity="center"
            android:layout_marginBottom="8dp" />

        <TextView
            android:id="@+id/text_status"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="🔴 DISCONNECTED"
            android:textColor="#ff6b6b"
            android:textSize="18sp"
            android:gravity="center"
            android:layout_marginBottom="4dp" />

        <TextView
            android:id="@+id/text_device_id"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Device: Unknown"
            android:textColor="#cccccc"
            android:textSize="12sp"
            android:gravity="center" />

    </LinearLayout>

    <!-- Camera Preview Section -->
    <androidx.camera.view.PreviewView
        android:id="@+id/preview_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="#000000" />

    <!-- Control Buttons Section -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="#1a1a1a"
        android:padding="16dp">

        <!-- Connection Controls -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginBottom="12dp">

            <Button
                android:id="@+id/button_connect"
                android:layout_width="0dp"
                android:layout_height="56dp"
                android:layout_weight="1"
                android:text="Connect"
                android:textColor="#ffffff"
                android:textSize="16sp"
                android:textStyle="bold"
                android:backgroundTint="#4CAF50"
                android:layout_marginEnd="8dp" />

            <Button
                android:id="@+id/button_settings"
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:text="⚙️"
                android:textSize="20sp"
                android:backgroundTint="#666666"
                android:layout_marginStart="8dp" />

        </LinearLayout>

        <!-- Streaming Control -->
        <Button
            android:id="@+id/button_stream"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="Start Streaming"
            android:textColor="#ffffff"
            android:textSize="16sp"
            android:textStyle="bold"
            android:backgroundTint="#2196F3"
            android:enabled="false"
            android:layout_marginBottom="12dp" />

        <!-- Info Section -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="• Connect to your server first\n• Camera preview shows what your phone sees\n• Make sure your server is running on the same network"
            android:textColor="#999999"
            android:textSize="12sp"
            android:layout_marginTop="12dp"
            android:lineSpacingMultiplier="1.2" />

    </LinearLayout>

</LinearLayout>